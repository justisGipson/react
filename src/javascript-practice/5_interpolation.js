const username = 'Justis';
console.log(`${username} is a true JS wizard!`);

console.log(`Hey students,
what bugs can
${username}
fix today?`)

const person = {
    username: 'Justis',
    profession: 'Secret Agent'
}
console.log(`${person.username}, ${person.profession} extraordinaire.`)